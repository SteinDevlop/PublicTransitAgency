name: Build and Push Frontend

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Lista todo en src/frontend
        run: ls -lR ./src/frontend
