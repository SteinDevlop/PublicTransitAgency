CREATE TABLE [dbo].[TipoTransporte] (
    [ID]             INT          NOT NULL,
    [TipoTransporte] VARCHAR (20) NOT NULL,
    CONSTRAINT [PK_TipoTransporte] PRIMARY KEY CLUSTERED ([ID] ASC)
);


GO

