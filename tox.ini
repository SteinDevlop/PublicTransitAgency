[tox]
envlist = py

[testenv]
deps =
    pytest
    coverage
commands =
    coverage run -m pytest
    coverage xml -o cobertura-coverage.xml --format=cobertura
 
[coverage:run]
relative_files = True
source = src/
branch = True

