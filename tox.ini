[tox]
envlist = py

[testenv]
deps =
    pytest
    pytest-cov
    pytest-cobertura
commands =
    pytest --cov=src --cov-report=xml:cobertura-coverage.xml
 
[coverage:run]
relative_files = True
source = src/
branch = True

